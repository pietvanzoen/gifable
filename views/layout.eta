<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="/public/style.css" />
    <title>🦩 Gifme</title>
  </head>
  <body class="<%= it.bodyClass || '' %>">
    <header>
      <a href="#main" class="visually-hidden">Skip to content</a>
      <h1>Gifme</h1>
      <% if (it.showNav !== false) { %>
      <nav>
        <a class="<%= it.view === 'search' ? 'current' : '' %>" href="/"
          >Search</a
        >
        <a class="<%= it.view === 'add' ? 'current' : '' %>" href="/add">Add</a>
        <a href="/logout">Logout</a>
      </nav>
      <% } else if (!it.signupDisabled) { %>
      <nav>
        <a class="<%= it.view === 'login' ? 'current' : '' %>" href="/login"
          >Login</a
        >
        <a class="<%= it.view === 'signup' ? 'current' : '' %>" href="/signup"
          >Signup</a
        >
      </nav>
      <% } %>
    </header>

    <%~ it.body %>
    <footer>
      <small
        >Build:
        <a
          href="https://github.com/pietvanzoen/gifme/tree/<%= it.buildSHA === 'dev'  ? 'main' : it.buildSHA %>"
          ><%= it.buildSHA %></a
        >
        |
        <a href="https://github.com/pietvanzoen/gifme/issues">Issues</a></small
      >
    </footer>
  </body>
</html>
